# Existing content before line 24
# Existing content... (not shown here)

modules_dir = coalesce(get_env("MODULES_DIR", ""), "modules/v1")
# Existing content after line 24
# Existing content... (not shown here)
